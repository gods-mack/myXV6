#include "types.h"
#include "stat.h"
#include "user.h"

char buf[512];


int
main(int argc, char *argv[])
{
  int fd, i;

  if(argc <= 1){
    wc(0, "");
    exit();
  }

  while(read(f))

    exit();
}
